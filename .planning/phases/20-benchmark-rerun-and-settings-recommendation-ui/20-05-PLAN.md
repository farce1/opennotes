---
phase: 20-benchmark-rerun-and-settings-recommendation-ui
plan: 05
type: execute
wave: 3
depends_on: [01, 02, 03, 04]
files_modified:
  - src/data/model-benchmarks.json
  - .planning/milestones/v1.1-phases/13-llm-quality-tuning/BENCHMARK.md
  - .planning/phases/20-benchmark-rerun-and-settings-recommendation-ui/runs/phi4-mini/quality/15min.md
  - .planning/phases/20-benchmark-rerun-and-settings-recommendation-ui/runs/phi4-mini/quality/45min.md
  - .planning/phases/20-benchmark-rerun-and-settings-recommendation-ui/runs/phi4-mini/quality/90min.md
  - .planning/phases/20-benchmark-rerun-and-settings-recommendation-ui/runs/llama3.2-3b/quality/15min.md
  - .planning/phases/20-benchmark-rerun-and-settings-recommendation-ui/runs/llama3.2-3b/quality/45min.md
  - .planning/phases/20-benchmark-rerun-and-settings-recommendation-ui/runs/llama3.2-3b/quality/90min.md
  - README.md
autonomous: false
requirements:
  - BENCH-01
  - BENCH-02
  - BENCH-03
  - BENCH-04
requirements_addressed:
  - BENCH-01
  - BENCH-02
  - BENCH-03
  - BENCH-04
user_setup:
  - service: ollama
    why: "The benchmark harness runs live inference against a local Ollama daemon to capture quality + speed metrics for phi4-mini and llama3.2:3b"
    env_vars: []
    dashboard_config:
      - task: "Install Ollama ≥ 0.5.0 from https://ollama.com/download"
        location: "Maintainer's machine"
      - task: "Start the Ollama daemon (`ollama serve`) in a separate terminal — harness pre-flights `curl http://localhost:11434/api/tags`"
        location: "Maintainer's machine"
      - task: "Free ~6 GB disk for the two model pulls (phi4-mini ~3 GB, llama3.2:3b ~2 GB)"
        location: "Maintainer's machine"
  - service: python
    why: "Quality scoring uses the v1.1 Python evaluator (.planning/milestones/v1.1-phases/13-llm-quality-tuning/eval/evaluate.py) reused as-is"
    env_vars: []
    dashboard_config:
      - task: "Install Python ≥ 3.10 (python3, python, or py launcher on PATH)"
        location: "Maintainer's machine"
tags:
  - benchmarks
  - maintainer-machine
  - manual-execution
  - data-commit
must_haves:
  truths:
    - "`src/data/model-benchmarks.json` contains live scored rows for phi4-mini (verdict: recommended) AND llama3.2:3b (verdict: alternate) measured on the maintainer's machine"
    - "v1.1 BENCHMARK.md no longer contains PENDING for iteration-2 phi4-mini rows; iterations 0 and 1 retain their PENDING with an unrecoverable footnote"
    - "Hardware-tier object is populated with real values from the maintainer's machine — NOT the skeleton placeholders"
    - "The Settings UI shows the `· Recommended` suffix on phi4-mini in `bun run dev` after the JSON is committed"
    - "README.md `<!-- BEGIN:BENCHMARK_TABLE -->` block is regenerated from the live JSON via `bun run benchmark:render-readme`"
    - "The 6 quality-pass canonical outputs (`runs/<model>/quality/<transcript>.md` × 2 models × 3 transcripts) are committed; warmup/ and speed/ subdirs are NOT committed (excluded by Plan 03's .gitignore)"
    - "Maintainer reviews `[hardware-tier] cpu=...` log line for PII (T-20-03 mitigation) before committing"
  artifacts:
    - path: "src/data/model-benchmarks.json"
      provides: "Live single source of truth — 2 model rows, real hardware_tier, real generated timestamp + git SHA"
      contains: '"phi4-mini"'
    - path: ".planning/milestones/v1.1-phases/13-llm-quality-tuning/BENCHMARK.md"
      provides: "Iteration-2 phi4-mini rows backfilled; iterations 0/1 retain PENDING + unrecoverable footnote"
      contains: "unrecoverable"
    - path: ".planning/phases/20-benchmark-rerun-and-settings-recommendation-ui/runs/phi4-mini/quality/45min.md"
      provides: "One of 6 committed quality-pass audit outputs"
  key_links:
    - from: "src/data/model-benchmarks.json"
      to: "src/components/settings/SummarySection.tsx"
      via: "module-scope static JSON import (Plan 02)"
      pattern: "import benchmarksRaw"
    - from: "src/data/model-benchmarks.json"
      to: "README.md (BENCHMARK_TABLE block)"
      via: "scripts/render-benchmark-readme.mjs generator (Plan 04)"
      pattern: "BEGIN:BENCHMARK_TABLE"
---

<objective>
**This is the maintainer-machine-only execution wave.** Plans 01-04 created the validator, the Settings UI rewire, the harness script, and the README generator. This plan instantiates them by:

1. Running `bun run benchmark` on the maintainer's machine with Ollama serving and Python ≥ 3.10 on PATH. The harness pulls phi4-mini + llama3.2:3b, runs warmup + quality + speed + E2E passes per model (~30–60 minutes total), writes `src/data/model-benchmarks.json` and backfills BENCHMARK.md.
2. Reviewing the generated JSON for hardware-tier PII (T-20-03 mitigation) and overall sanity.
3. Running `bun run benchmark:render-readme` to regenerate the README table.
4. Running `bun run build` + `bun run test` to confirm Plan 02's Settings UI accepts the live JSON.
5. Manually verifying via `bun run dev` that phi4-mini shows the `· Recommended` suffix in Settings → Summary.
6. Committing the generated files.

Purpose: Closes the data half of BENCH-01, BENCH-02, BENCH-03, BENCH-04. Without this wave, Phase 20 ships an empty skeleton — the JSON exists structurally but carries no real measurements.

Output: `src/data/model-benchmarks.json` (overwritten with live data), the v1.1 BENCHMARK.md backfill, 6 committed quality-pass audit `.md` files (2 models × 3 transcripts), and the regenerated README BENCHMARK_TABLE block.

**Why autonomous: false:** The harness REQUIRES a live Ollama daemon (separate process), the maintainer's actual hardware for the captured tier, ~6 GB disk for the model pulls, and ~45 min of compute that exceeds Claude's tool-call budget. Claude cannot perform these steps. This plan is structured as a `checkpoint:human-action` followed by a `checkpoint:human-verify` for the committed output.
</objective>

<execution_context>
@/Users/impera/Documents/GitHub/opennotes/.claude/get-shit-done/workflows/execute-plan.md
@/Users/impera/Documents/GitHub/opennotes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/20-benchmark-rerun-and-settings-recommendation-ui/20-CONTEXT.md
@.planning/phases/20-benchmark-rerun-and-settings-recommendation-ui/20-RESEARCH.md
@.planning/phases/20-benchmark-rerun-and-settings-recommendation-ui/20-PATTERNS.md
@.planning/phases/20-benchmark-rerun-and-settings-recommendation-ui/20-VALIDATION.md
@.planning/phases/20-benchmark-rerun-and-settings-recommendation-ui/20-01-SUMMARY.md
@.planning/phases/20-benchmark-rerun-and-settings-recommendation-ui/20-02-SUMMARY.md
@.planning/phases/20-benchmark-rerun-and-settings-recommendation-ui/20-03-SUMMARY.md
@.planning/phases/20-benchmark-rerun-and-settings-recommendation-ui/20-04-SUMMARY.md

@scripts/benchmark-models.mjs
@scripts/render-benchmark-readme.mjs
@src/data/model-benchmarks.json
@src/lib/benchmarks.ts
@.planning/milestones/v1.1-phases/13-llm-quality-tuning/BENCHMARK.md
@.planning/phases/20-benchmark-rerun-and-settings-recommendation-ui/runs/.gitignore
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Maintainer runs the benchmark harness on their machine</name>
  <read_first>
    - .planning/phases/20-benchmark-rerun-and-settings-recommendation-ui/20-CONTEXT.md (D-01, D-21, D-22a, D-23, D-24, D-34, D-35, D-36)
    - .planning/phases/20-benchmark-rerun-and-settings-recommendation-ui/20-RESEARCH.md §"Pitfall 7" (determinism caveat — re-runs should be within ±2.0 quality_score) + §"Pitfall 11" (Ollama version floor) + §"Environment Availability" table
    - .planning/phases/20-benchmark-rerun-and-settings-recommendation-ui/20-VALIDATION.md §"Sampling Rate" — "Before /gsd-verify-work: full suite green + bun run build clean + manual harness dry-run"
    - scripts/benchmark-models.mjs (the script being invoked)
  </read_first>
  <what-built>
    Plans 01-04 are merged: the validator (`src/lib/benchmarks.ts`), Settings UI rewire (`SummarySection.tsx`), harness script (`scripts/benchmark-models.mjs`), README generator (`scripts/render-benchmark-readme.mjs`), and the empty skeleton (`src/data/model-benchmarks.json` with `models: []`).
    No live data has been produced yet. The Settings UI shows NO `· Recommended` suffix on any model (skeleton has empty models[]). The README BENCHMARK_TABLE block is empty (header + divider only).
  </what-built>
  <how-to-verify>
**Maintainer prerequisites (verify each before starting):**
1. `node --version` reports ≥ 22.x (project requirement).
2. `python3 --version` (or `python --version`, or `py --version`) reports ≥ 3.10.
3. `ollama --version` reports ≥ 0.5.0. Upgrade at https://ollama.com/download if older.
4. **In a separate terminal**, start the Ollama daemon: `ollama serve`. Confirm it responds: `curl -fsS http://localhost:11434/api/tags` returns a 200 with a (possibly empty) `{"models":[...]}` JSON body.
5. Free disk space: at least 6 GB available for the two model pulls (phi4-mini ~3 GB, llama3.2:3b ~2 GB, plus working space).
6. Git working tree is clean (`git status --porcelain` is empty) — the harness will still run with a dirty tree but will append `-dirty` to `generator_git_sha` and warn. For a release-ready JSON, commit first.

**Execution steps (estimated 30-60 minutes wall-clock; harness is sequential by Pitfall 7):**

```bash
# From the repo root:
cd /Users/impera/Documents/GitHub/opennotes  # or wherever the repo lives

# Full sweep (both models):
bun run benchmark

# Or — if iterating — single model:
# bun run benchmark --model phi4-mini
# (Partial reruns preserve the other model's row verbatim per D-22.)
```

**What you should see during the run:**

1. `[preflight] python=python3 ollama=0.5.x git=<sha> target_models=phi4-mini,llama3.2:3b`
2. `[hardware-tier] cpu="Apple M3 Pro" ram_gb=36 gpu="Apple M3 Pro (integrated, Metal)" os="darwin"` (your hardware values; **sanity-check for PII per T-20-03 — no hostname/username/IP should appear**)
3. `=== Benchmarking phi4-mini (verdict: recommended) ===`
4. `[pull] ollama pull phi4-mini` (slow first time; near-instant on rerun)
5. `[warmup] phi4-mini`
6. `[quality] phi4-mini / 15min`, then `/ 45min`, then `/ 90min` (the 90min run triggers the chunked path — takes the longest)
7. Per-transcript: `→ action=...% decisions=...% keypoints=...% sections=true|false score=...`
8. `[speed] phi4-mini run 1/5` through `run 5/5`
9. `[e2e] phi4-mini (production num_predict: -1)`
10. `=== Benchmarking llama3.2:3b (verdict: alternate) ===` — same shape.
11. `[write] /…/src/data/model-benchmarks.json (2 model rows)`
12. `[backfill] BENCHMARK.md iteration-2 rows replaced (3/3) and footnote ensured.`
13. `[done] benchmark run complete.`

**If the run fails mid-flight:** the temp files (`*.bench.tmp`) are orphaned but the targets are untouched. Re-run from scratch (`bun run benchmark`). Per the script's catch handler, you'll see `One or more files may be in an inconsistent state — review git diff.` — `runs/` may have partial outputs; clean them with `git clean -fd .planning/phases/20-.../runs/` before re-running.

**If a pre-flight check fails:** fix the named prerequisite and re-run. Common failures:
- "Ollama not running at http://localhost:11434 — run 'ollama serve' first" → start the daemon.
- "openNotes benchmark harness requires Ollama ≥ 0.5.0" → upgrade Ollama.
- "openNotes benchmark harness requires Python ≥ 3.10 on PATH" → install/upgrade Python.

**Determinism check** (per D-23, optional but recommended): re-run `bun run benchmark` once. Compare the `quality_score` value for each model — they should be within ±2.0 of the first run. If drift exceeds ±5, investigate per RESEARCH §Pitfall 7 (likely cause: a prompt template edit in `src-tauri/src/llm/mod.rs` landed between runs).

**Resume signal:** type `harness-complete` once `[done] benchmark run complete.` has been printed and `git status` shows `src/data/model-benchmarks.json` modified plus new `.md` files in `.planning/phases/20-.../runs/<model>/quality/`. If the run failed, type `harness-failed: <reason>` so the executor can diagnose.
  </how-to-verify>
  <resume-signal>Type `harness-complete` once the harness has completed `[done]` and committed-to-disk files are present, or `harness-failed: <reason>` if it failed.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Review the live JSON, render README table, run build + tests, prepare audit-set commit</name>
  <read_first>
    - src/data/model-benchmarks.json (now overwritten with live data after Task 1)
    - .planning/milestones/v1.1-phases/13-llm-quality-tuning/BENCHMARK.md (now backfilled after Task 1)
    - scripts/render-benchmark-readme.mjs (Plan 04 — the generator we invoke here)
    - .planning/phases/20-benchmark-rerun-and-settings-recommendation-ui/20-VALIDATION.md §"Per-Task Verification Map" + §"Manual-Only Verifications"
    - .planning/phases/20-benchmark-rerun-and-settings-recommendation-ui/20-CONTEXT.md (D-22a — committed audit set scope)
    - .planning/phases/20-benchmark-rerun-and-settings-recommendation-ui/runs/.gitignore (ensures speed/ and warmup/ are NOT committed)
  </read_first>
  <files>README.md, .planning/phases/20-benchmark-rerun-and-settings-recommendation-ui/runs/phi4-mini/quality/15min.md, .planning/phases/20-benchmark-rerun-and-settings-recommendation-ui/runs/phi4-mini/quality/45min.md, .planning/phases/20-benchmark-rerun-and-settings-recommendation-ui/runs/phi4-mini/quality/90min.md, .planning/phases/20-benchmark-rerun-and-settings-recommendation-ui/runs/llama3.2-3b/quality/15min.md, .planning/phases/20-benchmark-rerun-and-settings-recommendation-ui/runs/llama3.2-3b/quality/45min.md, .planning/phases/20-benchmark-rerun-and-settings-recommendation-ui/runs/llama3.2-3b/quality/90min.md</files>
  <behavior>
    - The live `src/data/model-benchmarks.json` exists with two model rows (phi4-mini, llama3.2:3b) and the schema validator (`src/lib/benchmarks.ts`) accepts it (proved indirectly by `bun run build` passing).
    - The README BENCHMARK_TABLE block now contains real data (2 rows + the footnote with real hardware/methodology values).
    - All 6 quality-pass canonical outputs exist in `runs/<path-safe-model>/quality/<transcript>.md` and are tracked by git (NOT excluded by the .gitignore).
    - The `runs/<path-safe-model>/warmup/` and `runs/<path-safe-model>/speed/` directories exist on disk (if the harness wrote them) but are excluded from git per the .gitignore (D-22a).
    - The v1.1 BENCHMARK.md no longer contains PENDING in iteration-2 phi4-mini rows; iterations 0 and 1 retain PENDING with the unrecoverable footnote anchored by `<!-- v1.3 BACKFILL FOOTNOTE -->`.
    - The Vite production build accepts the live JSON: `bun run build` exits 0 with no TypeScript errors.
    - Vitest suite remains green: `bun run test` exits 0.
    - Grep gate: `bun run build && grep -lc 'phi4-mini' dist/assets/*.js | awk -F: '{s+=\$NF} END{exit !(s>=1)}'` — the phi4-mini string from the JSON appears in the bundled JS chunk (proves BENCH-04 — Vite static import works end-to-end).
  </behavior>
  <action>
**Step 1 — Hardware-tier PII sanity check (T-20-03 mitigation):**

Open `src/data/model-benchmarks.json` and inspect the `hardware_tier` object. It should contain ONLY: `cpu_model` (e.g., "Apple M3 Pro"), `total_ram_gb` (integer), `gpu_present` (boolean), `gpu_model` (e.g., "Apple M3 Pro (integrated, Metal)" or null), `os` ("darwin" | "win32" | "linux"). It MUST NOT contain a hostname, username, IP address, MAC, or any other identifying info. If anything unexpected appears, STOP and inspect `scripts/benchmark-models.mjs` `detectHardwareTier` — the script should have captured only the 5 fields. If clean, proceed.

Run the verifier:
```bash
node -e "const d=JSON.parse(require('fs').readFileSync('src/data/model-benchmarks.json','utf8')); const keys=Object.keys(d.hardware_tier).sort(); const allowed=['cpu_model','gpu_model','gpu_present','os','total_ram_gb']; if (JSON.stringify(keys) !== JSON.stringify(allowed)) { console.error('PII LEAK: unexpected hardware_tier keys:', keys); process.exit(1); } console.log('hardware_tier clean:', keys.join(','));"
```

Expected output: `hardware_tier clean: cpu_model,gpu_model,gpu_present,os,total_ram_gb`. Exit 0.

**Step 2 — Render the README table from the live JSON:**

```bash
bun run benchmark:render-readme
```

Confirm the output: `[render] README.md benchmark table updated (2 model rows).` (count may be `1` if `--model` was used partially). Re-run once more to confirm idempotency: `bun run benchmark:render-readme && git diff --quiet README.md` should print "[render] README.md already up to date — no write needed." and exit 0 with no diff.

**Step 3 — Run the build and test suite to confirm Plan 02's Settings UI accepts the live JSON:**

```bash
bun run build
```

Must exit 0 with no TypeScript errors. The live JSON passing through `validateBenchmarks(benchmarksRaw)` at `SummarySection.tsx` module load is the gating check — if the harness produced a malformed row, this build fails. If it fails, inspect the validator's error message (it includes a path like `$.models[0].quality.per_transcript.15min.action_items_pct`) and fix either the harness or the JSON by hand (a hand-edit is acceptable for a one-shot fix; the next `bun run benchmark` would regenerate the file).

Then:
```bash
bun run test
```

Must exit 0. Plan 01's Vitest suite continues to assert validator correctness against the live JSON.

**Step 4 — Verify Vite bundle contains the model names (BENCH-04 build-smoke check from VALIDATION.md):**

```bash
bun run build && grep -lc 'phi4-mini' dist/assets/*.js
```

At least one `dist/assets/*.js` file should contain the string `phi4-mini` (the JSON bundled into the Settings UI chunk).

**Step 5 — Verify the BENCHMARK.md backfill:**

```bash
grep -c "iteration 2.*PENDING" .planning/milestones/v1.1-phases/13-llm-quality-tuning/BENCHMARK.md || echo "no iteration-2 PENDINGs remain"
grep -A2 "unrecoverable" .planning/milestones/v1.1-phases/13-llm-quality-tuning/BENCHMARK.md | head -5
```

Expected: zero iteration-2 PENDINGs (the regex from `bunch run benchmark` replaced them with measured percentages); the unrecoverable footnote is present near the iteration table. Iterations 0 and 1 retain their PENDING rows — that's intentional per D-36.

**Step 6 — Prepare the audit-set commit. Inspect `git status` and confirm the diff scope:**

Expected modified/new files:
- `src/data/model-benchmarks.json` (modified — live data overwrites skeleton)
- `.planning/milestones/v1.1-phases/13-llm-quality-tuning/BENCHMARK.md` (modified — iteration-2 backfill + footnote)
- `README.md` (modified — BENCHMARK_TABLE block populated)
- 6 new files under `.planning/phases/20-benchmark-rerun-and-settings-recommendation-ui/runs/<model>/quality/<transcript>.md` (committed audit set per D-22a)
- `.planning/phases/20-benchmark-rerun-and-settings-recommendation-ui/runs/.gitignore` (already committed by Plan 03, untouched here)

Files that should NOT appear in `git status` (excluded by Plan 03's `.gitignore`):
- `.planning/phases/20-.../runs/<model>/warmup/*`
- `.planning/phases/20-.../runs/<model>/speed/*`

Verify with `git status --ignored | grep -E "warmup|speed"`. The output should list the excluded directories under "Ignored files".

If extra unexpected files appear (e.g., a `runs/<some-other-model>/` directory from a previous experimental run), use `git clean -dn .planning/phases/20-.../runs/` to dry-run a cleanup, then `git clean -df` if the dry-run looks correct. **Do NOT commit unrelated test outputs.**

This task does NOT do the git commit — that's the verification checkpoint in Task 3 where the maintainer reviews the staged diff before pushing.
  </action>
  <verify>
    <automated>cd /Users/impera/Documents/GitHub/opennotes &amp;&amp; node -e "const d=JSON.parse(require('fs').readFileSync('src/data/model-benchmarks.json','utf8')); if (!Array.isArray(d.models) || d.models.length<2) process.exit(1); const names=d.models.map(m=>m.name).sort(); if (!names.includes('phi4-mini') || !names.includes('llama3.2:3b')) {console.error('lineup mismatch:', names); process.exit(1)}; if (d.models.find(m=>m.name==='phi4-mini').verdict !== 'recommended') {console.error('phi4-mini verdict not recommended'); process.exit(1)}; const keys=Object.keys(d.hardware_tier).sort(); const allowed=['cpu_model','gpu_model','gpu_present','os','total_ram_gb']; if (JSON.stringify(keys)!==JSON.stringify(allowed)) {console.error('PII LEAK in hardware_tier:', keys); process.exit(1)}; console.log('JSON OK: models=', names.join(','), 'phi4-mini.verdict=recommended hardware_tier_clean')" &amp;&amp; bun run benchmark:render-readme &amp;&amp; bun run benchmark:render-readme &amp;&amp; git diff --quiet README.md &amp;&amp; bun run build &amp;&amp; bun run test 2>&amp;1 | tail -5 &amp;&amp; grep -lc "phi4-mini" dist/assets/*.js | awk -F: '{s+=$NF} END{if (s<1) exit 1}' &amp;&amp; ! grep -E "iteration 2.*PENDING|2 \(tuned prompt\).*PENDING" .planning/milestones/v1.1-phases/13-llm-quality-tuning/BENCHMARK.md &amp;&amp; grep -q "unrecoverable" .planning/milestones/v1.1-phases/13-llm-quality-tuning/BENCHMARK.md &amp;&amp; test -f .planning/phases/20-benchmark-rerun-and-settings-recommendation-ui/runs/phi4-mini/quality/45min.md &amp;&amp; test -f .planning/phases/20-benchmark-rerun-and-settings-recommendation-ui/runs/llama3.2-3b/quality/45min.md</automated>
  </verify>
  <acceptance_criteria>
    - JSON has ≥ 2 model rows with names `phi4-mini` and `llama3.2:3b`: see automated verifier
    - phi4-mini row has `verdict: "recommended"` and llama3.2:3b has `verdict: "alternate"`
    - `hardware_tier` keys are EXACTLY the 5 allowed: `cpu_model`, `gpu_model`, `gpu_present`, `os`, `total_ram_gb` (T-20-03 gate)
    - `generator_git_sha` is a 40-char hex (optionally with `-dirty` suffix): `node -e "const d=JSON.parse(require('fs').readFileSync('src/data/model-benchmarks.json','utf8')); if(!/^[0-9a-f]{40}(-dirty)?$/.test(d.generator_git_sha)) process.exit(1)"`
    - README regenerated and idempotent: second `bun run benchmark:render-readme` produces zero diff
    - `bun run build` exits 0
    - `bun run test` exits 0 (Plan 01's validator suite continues to pass against the live JSON)
    - Vite bundle contains the model strings: `bun run build && grep -lc 'phi4-mini' dist/assets/*.js | awk -F: '{s+=$NF} END{if (s<1) exit 1}'`
    - BENCHMARK.md no longer has iteration-2 PENDINGs: `! grep -E "iteration 2.*PENDING|2 \\(tuned prompt\\).*PENDING" .planning/milestones/v1.1-phases/13-llm-quality-tuning/BENCHMARK.md`
    - BENCHMARK.md retains the unrecoverable footnote: `grep -q "unrecoverable" .planning/milestones/v1.1-phases/13-llm-quality-tuning/BENCHMARK.md`
    - All 6 quality-pass canonical outputs exist: each of the 6 files under `runs/<path-safe-model>/quality/<transcript>.md` exists
    - Excluded artifacts are NOT staged: `git status --porcelain | grep -E "runs/.*/(warmup|speed)/" | wc -l | awk '$1==0{exit 0} {exit 1}'`
  </acceptance_criteria>
  <done>The repo is in the state ready for commit: live JSON + backfilled BENCHMARK.md + regenerated README table + 6 quality-pass audit outputs + clean build + clean tests + no PII in hardware_tier + no leaked warmup/speed outputs.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Maintainer manually verifies the Settings UI badge + reviews and commits the audit set</name>
  <read_first>
    - .planning/phases/20-benchmark-rerun-and-settings-recommendation-ui/20-VALIDATION.md §"Manual-Only Verifications" (the 4 manual checks — most relevant: "Removing the verdict: recommended row makes the badge disappear without a code change", and the harness end-to-end dry run)
    - .planning/phases/20-benchmark-rerun-and-settings-recommendation-ui/20-CONTEXT.md (D-22a — committed audit set scope)
  </read_first>
  <what-built>
    Task 1 produced live measurement data on the maintainer's machine; Task 2 confirmed the JSON validates, the build is clean, the README is regenerated, and the v1.1 backfill is in place. Nothing is committed yet — the diff is staged-ready in the working tree.
  </what-built>
  <how-to-verify>
**Manual verification step 1 — Settings UI badge (ROADMAP P20 Success Criterion 4 litmus test):**

1. Run `bun run dev` in a terminal.
2. Wait for the dev server to come up (printed URL — usually http://localhost:1420).
3. Open the app, navigate to Settings → Summary.
4. In the "Model" dropdown (or the model selection control), inspect the labels:
   - `phi4-mini` (or `phi4-mini:latest` depending on what Ollama has locally) should display with a trailing ` · Recommended` suffix.
   - `llama3.2:3b` should NOT display the suffix (it has `verdict: "alternate"`).
   - Other models the user has locally pulled (if any) should NOT display the suffix unless they happen to share a name with a `verdict: "recommended"` row in the JSON.
5. **Litmus test the "no code change required" property:** edit `src/data/model-benchmarks.json` and change phi4-mini's `verdict` from `"recommended"` to `"alternate"`. Save. Vite HMR reloads. Verify NO model now shows the suffix. Then change it back to `"recommended"`, save, verify the suffix returns. REVERT any test changes — do not commit them.
6. Stop the dev server (Ctrl-C).

**Manual verification step 2 — Review the staged diff before commit:**

```bash
git status
git diff src/data/model-benchmarks.json | head -40
git diff .planning/milestones/v1.1-phases/13-llm-quality-tuning/BENCHMARK.md | head -40
git diff README.md | head -40
ls .planning/phases/20-benchmark-rerun-and-settings-recommendation-ui/runs/
```

Sanity-check that:
- `model-benchmarks.json` has realistic numbers (quality_score between ~30 and ~100; tokens_per_sec roughly in the 5-100 range depending on hardware; e2e_summary_seconds roughly 5-300s)
- BENCHMARK.md iteration-2 rows have measured percentages, NOT PENDINGs
- README has the populated table with both model rows + the footnote
- `runs/` has the 6 quality-pass `.md` files under the 2 model dirs; warmup/speed/ are NOT staged (verify with `git status --porcelain | grep runs`)

**Manual verification step 3 — Commit the audit set:**

```bash
git add src/data/model-benchmarks.json
git add .planning/milestones/v1.1-phases/13-llm-quality-tuning/BENCHMARK.md
git add README.md
git add .planning/phases/20-benchmark-rerun-and-settings-recommendation-ui/runs/phi4-mini/quality/15min.md
git add .planning/phases/20-benchmark-rerun-and-settings-recommendation-ui/runs/phi4-mini/quality/45min.md
git add .planning/phases/20-benchmark-rerun-and-settings-recommendation-ui/runs/phi4-mini/quality/90min.md
git add .planning/phases/20-benchmark-rerun-and-settings-recommendation-ui/runs/llama3.2-3b/quality/15min.md
git add .planning/phases/20-benchmark-rerun-and-settings-recommendation-ui/runs/llama3.2-3b/quality/45min.md
git add .planning/phases/20-benchmark-rerun-and-settings-recommendation-ui/runs/llama3.2-3b/quality/90min.md

git commit -m "$(cat <<'EOF'
feat(20): live benchmark data — phi4-mini + llama3.2:3b on <hardware tier>

Run the warmup + N=5 + median harness from Plan 03 on the maintainer's machine.
Writes src/data/model-benchmarks.json with two scored rows (phi4-mini recommended,
llama3.2:3b alternate), backfills v1.1 BENCHMARK.md iteration-2 rows and the
unrecoverable footnote for iterations 0/1, and regenerates the README table.

Closes BENCH-01, BENCH-02, BENCH-03, BENCH-04 (data half).
EOF
)"
```

(Adjust the commit message's `<hardware tier>` placeholder to your real CPU model.)

**Resume signal options:**
- `verified-and-committed` — Settings UI shows the badge correctly, the staged diff is clean, the commit landed.
- `issue: <description>` — if any verification step failed (e.g., badge doesn't appear, hardware_tier looks wrong, build broke). Describe the issue; the executor diagnoses against the relevant plan.
  </how-to-verify>
  <resume-signal>Type `verified-and-committed` or `issue: <description>`</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Maintainer's machine → public repo (commit + push) | The live measurement data and the audit `.md` files cross from a private workstation into the public repository. T-20-03 (PII in hardware-tier) is the relevant threat. |
| Live Ollama daemon → harness | Loopback HTTP; same trust model as Plan 03. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-03 | Information Disclosure | `src/data/model-benchmarks.json` `hardware_tier` field committed to public repo | mitigate | Task 2 Step 1 runs a structural check that the only keys present are `{cpu_model, gpu_model, gpu_present, os, total_ram_gb}`. Task 3 Step 2 includes a manual review of the diff before commit. The harness itself (Plan 03) emits a `[hardware-tier] cpu=... ram_gb=... gpu=... os=...` log line during the run so the maintainer can sanity-check in real time. Severity: low. |
| T-20-04 | Denial-of-Service | Atomic write interrupted mid-rename | mitigate | Plan 03 implements the atomic temp-rename pattern; the harness either produces a complete output or leaves the target untouched. If interrupted, the maintainer re-runs `bun run benchmark`. Severity: low. |
</threat_model>

<verification>
- Task 1 (manual): `[done] benchmark run complete.` printed; live JSON + backfilled BENCHMARK.md + 6 new audit `.md` files present
- Task 2 (automated): the consolidated verifier in the `<verify>` block exits 0 — JSON has 2 rows, hardware_tier keys are exactly the 5 allowed, build/tests green, README idempotent, BENCHMARK.md backfilled and footnote present, all 6 audit outputs exist
- Task 3 (manual): Settings UI shows `· Recommended` on phi4-mini and not on llama3.2:3b in `bun run dev`; the JSON-edit litmus test confirms removing the verdict makes the badge disappear without code changes; staged diff reviewed and committed
</verification>

<success_criteria>
- ROADMAP P20 Success Criterion 1 (v1.1 BENCHMARK.md no longer contains PENDING for iteration-2 phi4-mini; rows carry hardware-tier + methodology metadata): closed.
- ROADMAP P20 Success Criterion 2 (≥1 alternate model with scored rows): closed — llama3.2:3b is the alternate.
- ROADMAP P20 Success Criterion 3 (src/data/model-benchmarks.json exists, Vite static import, generated timestamp + schema_version, single source of truth): closed — the Settings UI and README both read this file.
- ROADMAP P20 Success Criterion 4 (SummarySection.tsx badge from JSON lookup; no hard-coded phi4-mini predicate; removing/renaming the recommended row changes the badge without a code change): closed — manually verified in Task 3 Step 1 step 5.
- ROADMAP P20 Success Criterion 5 (JSON is dependency-free at user-install time — no Python, no fixtures, no Ollama runtime): closed — the JSON is just text, Vite bundles it, users never see the harness.
- BENCH-01, BENCH-02, BENCH-03, BENCH-04 data half closed.
- No PII in `hardware_tier`.
- 6 quality-pass audit `.md` files committed; warmup/ and speed/ outputs gitignored.
</success_criteria>

<output>
After completion, create `.planning/phases/20-benchmark-rerun-and-settings-recommendation-ui/20-05-SUMMARY.md` documenting:
- The actual measured numbers (quality_score, tokens_per_sec, time_to_first_token_ms, e2e_summary_seconds) for both models — copy from the committed JSON
- The maintainer's `hardware_tier` (verbatim from the JSON)
- The `generator_git_sha` of the commit BEFORE this run (so future reproducibility checks can pin to the right code state)
- Confirmation of the manual badge-litmus-test outcome (badge appears on phi4-mini, disappears when verdict is changed in JSON, returns when reverted — no code changes)
- Any determinism observations across reruns (if multiple runs were performed for sanity)
- The exact commit SHA produced by Task 3 Step 3
</output>
