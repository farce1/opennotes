---
phase: 20-benchmark-rerun-and-settings-recommendation-ui
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/components/settings/SummarySection.tsx
autonomous: true
requirements:
  - BENCH-05
requirements_addressed:
  - BENCH-05
tags:
  - settings
  - ui
  - i18n
  - benchmarks
  - react
must_haves:
  truths:
    - "`formatModelLabel` no longer contains the hard-coded literal `'phi4-mini'`"
    - "`formatModelLabel` is a plain function — no `useTranslation` inside it (D-30a option a)"
    - "Removing or renaming the `verdict: \"recommended\"` row in the JSON changes the badge without any code change in this file"
    - "The i18n key `model_recommended` drives the badge label (no hard-coded English literal)"
    - "Module-scope `validateBenchmarks(benchmarksRaw)` runs once at import time and throws fail-fast on schema mismatch"
  artifacts:
    - path: "src/components/settings/SummarySection.tsx"
      provides: "Settings → Summary view; the `· Recommended` suffix predicate is now data-driven via BENCHMARKS.models.some(...)"
      contains: "BENCHMARKS.models.some"
  key_links:
    - from: "src/components/settings/SummarySection.tsx"
      to: "src/data/model-benchmarks.json"
      via: "module-scope static JSON import"
      pattern: "import benchmarksRaw from '../../data/model-benchmarks.json'"
    - from: "src/components/settings/SummarySection.tsx"
      to: "src/lib/benchmarks.ts"
      via: "named imports of validateBenchmarks + matchesBenchmarkModel"
      pattern: "from '../../lib/benchmarks'"
    - from: "formatModelLabel call sites (modelDropdownOptions, pullModelDropdownOptions)"
      to: "formatModelLabel"
      via: "second argument: t('model_recommended')"
      pattern: "formatModelLabel\\(model, t\\('model_recommended'\\)\\)"
---

<objective>
Rewire `src/components/settings/SummarySection.tsx` so the `· Recommended` badge suffix is driven by `src/data/model-benchmarks.json` lookup instead of the hard-coded `'phi4-mini'` literal. Pick D-30a option (a) — pass `recommendedLabel` as a second parameter to `formatModelLabel`. The badge visual is unchanged (no UI redesign per D-32).

Purpose: Closes BENCH-05 and ROADMAP P20 Success Criterion 4 ("removing or renaming the recommended row in JSON changes the badge without a code change"). Also fixes a latent i18n bug en route — the existing code uses a hard-coded English `" · Recommended"` literal; the new code uses `t('model_recommended')` which already has parity at `src/i18n/locales/{en,pl}/settings.json:147` (en: "Recommended", pl: "Zalecany").

Output: Single file modified. ~12 lines changed in `formatModelLabel`, 3 module-scope additions (the static JSON import, the validator import, the `const BENCHMARKS = ...` line), 2 call-site updates (the two `formatModelLabel(model)` call sites in `useMemo`s gain a second arg `t('model_recommended')` and `t` is added to the `useMemo` dep arrays).
</objective>

<execution_context>
@/Users/impera/Documents/GitHub/opennotes/.claude/get-shit-done/workflows/execute-plan.md
@/Users/impera/Documents/GitHub/opennotes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/20-benchmark-rerun-and-settings-recommendation-ui/20-CONTEXT.md
@.planning/phases/20-benchmark-rerun-and-settings-recommendation-ui/20-RESEARCH.md
@.planning/phases/20-benchmark-rerun-and-settings-recommendation-ui/20-PATTERNS.md
@.planning/phases/20-benchmark-rerun-and-settings-recommendation-ui/20-VALIDATION.md
@.planning/phases/20-benchmark-rerun-and-settings-recommendation-ui/20-01-SUMMARY.md

@src/components/settings/SummarySection.tsx
@src/lib/benchmarks.ts
@src/types/model-benchmarks.ts
@src/data/model-benchmarks.json
@src/i18n/locales/en/settings.json
@src/i18n/locales/pl/settings.json

<interfaces>
<!-- The interfaces this file already consumes from elsewhere. Use as-is — no exploration needed. -->

// From src/types/index.ts (already imported by SummarySection.tsx:9)
export interface OllamaModelInfo {
  name: string;
  parameterSize: string | null;
  downloadSize: string | null;
}

// From src/lib/benchmarks.ts (created in Plan 01)
export function validateBenchmarks(raw: unknown): BenchmarkData;
export function matchesBenchmarkModel(modelName: string, jsonRowName: string): boolean;

// From src/types/model-benchmarks.ts (created in Plan 01)
export interface BenchmarkData {
  schema_version: 1;
  generated: string;
  generator: string;
  generator_git_sha: string;
  hardware_tier: HardwareTier;
  methodology: Methodology;
  models: BenchmarkModel[];
}
export interface BenchmarkModel {
  name: string;
  verdict: Verdict;  // 'recommended' | 'alternate' | null
  quality: Quality;
  speed: Speed;
}

// From src/i18n/locales/{en,pl}/settings.json:147 (already present)
//   en: "model_recommended": "Recommended"
//   pl: "model_recommended": "Zalecany"
// Access via const { t } = useTranslation('settings'); then t('model_recommended').
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewire formatModelLabel and call sites in SummarySection.tsx</name>
  <read_first>
    - src/components/settings/SummarySection.tsx (the file being modified — read lines 1-50 for imports + formatModelLabel, lines 220-250 for the two call sites)
    - .planning/phases/20-benchmark-rerun-and-settings-recommendation-ui/20-CONTEXT.md (D-29, D-30, D-30a — locked to option (a); D-32 — no new UI components; D-33 — removed-row tolerance)
    - .planning/phases/20-benchmark-rerun-and-settings-recommendation-ui/20-RESEARCH.md §"Pitfall 5" (rules-of-hooks violation if useTranslation called inside formatModelLabel) + §"Settings UI rewire" + §Example 5 (verbatim before/after code)
    - .planning/phases/20-benchmark-rerun-and-settings-recommendation-ui/20-PATTERNS.md §"src/components/settings/SummarySection.tsx" (Target shape after rewire — copy verbatim)
    - src/lib/benchmarks.ts (the validator + normalizer that the new code imports)
    - src/data/model-benchmarks.json (the static asset the module-scope import targets)
    - src/i18n/locales/en/settings.json line 147 (`model_recommended` key — already present)
    - src/i18n/locales/pl/settings.json line 147 (PL parity — already present)
  </read_first>
  <files>src/components/settings/SummarySection.tsx</files>
  <behavior>
    - After the change, the `· Recommended` suffix appears on every model in the Settings → Summary dropdown whose name matches (via `matchesBenchmarkModel`) a JSON row with `verdict: "recommended"`.
    - With the Wave 1 skeleton JSON (`models: []`), NO model shows the badge — degraded behavior is correct, never crashes (D-33).
    - When Plan 05 commits the live JSON (phi4-mini with `verdict: "recommended"`, llama3.2:3b with `verdict: "alternate"`), phi4-mini shows the badge and llama3.2:3b does not.
    - Editing the JSON to remove or rename the `verdict: "recommended"` row makes the badge disappear without a code change (verified manually per the VALIDATION.md manual integration check).
    - `formatModelLabel(model)` becomes `formatModelLabel(model, recommendedLabel)`. Both `useMemo` call sites pass `t('model_recommended')` as the second arg and add `t` to their dependency arrays.
    - The module fails fast (synchronous throw at import time) if the committed JSON has a shape mismatch — this is the intended fail-fast behavior per D-29 and RESEARCH §Open Question 4.
  </behavior>
  <action>
**Apply the following exact edits to `src/components/settings/SummarySection.tsx`:**

**Edit 1 — Add module-scope imports and the validated `BENCHMARKS` constant** (insert after the existing imports block, around line 11, before the `type PullProgress` declaration):

Add these three lines immediately after the existing `import { Dropdown } from '../ui/Dropdown';` line (currently line 10):

```ts
import benchmarksRaw from '../../data/model-benchmarks.json';
import { matchesBenchmarkModel, validateBenchmarks } from '../../lib/benchmarks';

const BENCHMARKS = validateBenchmarks(benchmarksRaw);
```

Order the imports correctly: the JSON import is a relative module import so it joins the existing relative-import group (`../../contexts/...`, `../../hooks/...`, `../../lib/constants`, `../../types`, `../ui/Dropdown`). Place the `benchmarksRaw` import grouped with `../../lib/constants` and `../../types` (it is data, not a component) and the `validateBenchmarks` import alongside it. The `const BENCHMARKS = ...` line sits between the imports block and `type PullProgress` so it executes once at module load.

**Edit 2 — Rewrite `formatModelLabel`** (currently lines 30-37; replace the entire function).

Original code (currently at SummarySection.tsx line 35):
```ts
const rec = model.name === 'phi4-mini' || model.name === 'phi4-mini:latest' ? ' · Recommended' : '';
```

Replacement: change the function signature to take a second `recommendedLabel: string` parameter, and replace the hard-coded predicate with a JSON-driven lookup. The full new function body (per CONTEXT.md D-30 verbatim + D-30a option (a)):

```ts
function formatModelLabel(model: OllamaModelInfo, recommendedLabel: string): string {
  const download = model.downloadSize ? ` · ${model.downloadSize}` : '';
  const normalizedSize = model.parameterSize?.toLowerCase();
  const sizeIncludedInName = normalizedSize ? model.name.toLowerCase().endsWith(`:${normalizedSize}`) : false;
  const size = model.parameterSize && !sizeIncludedInName ? ` · ${model.parameterSize}` : '';
  const recommended = BENCHMARKS.models.some(
    (b) => b.verdict === 'recommended' && matchesBenchmarkModel(model.name, b.name),
  );
  const rec = recommended ? ` · ${recommendedLabel}` : '';
  return `${model.name}${download}${size}${rec}`;
}
```

Do NOT add `useTranslation` inside `formatModelLabel`. That would violate rules-of-hooks (RESEARCH §Pitfall 5). The caller passes the already-resolved string.

**Edit 3 — Update both `formatModelLabel` call sites** (currently lines 229-245 — the `modelDropdownOptions` and `pullModelDropdownOptions` `useMemo`s).

In `modelDropdownOptions` (currently `formatModelLabel(model)` at line 233), change the call to:
```ts
label: formatModelLabel(model, t('model_recommended')),
```

Then add `t` to its dependency array (currently `[modelOptions]` at line 235). The dep array becomes `[modelOptions, t]`.

Apply the identical edit to `pullModelDropdownOptions` (lines 238-245). The call site becomes `formatModelLabel(model, t('model_recommended'))` and the dep array becomes `[availablePullModels, t]`.

The `t` variable is already in scope at line 45 (`const { t } = useTranslation('settings');`); no new imports needed for i18n.

**Do NOT make any other changes:**
- Do NOT add a `recommended: boolean` field to `OllamaModelInfo` (CONTEXT.md "Reusable Assets" — wrong layering)
- Do NOT change `currentModel`, `DEFAULT_SETTINGS.ollamaModel`, or any startup model-selection logic (CONTEXT.md "Files this phase modifies" §`src/lib/constants.ts`)
- Do NOT add a new UI component or change the visual presentation of the badge (D-32)
- Do NOT wrap the import in a try/catch — D-29 + RESEARCH §Open Question 4: fail-fast at module-load is the intended behavior

**Rationale for D-30a option (a) over option (b):** RESEARCH §Pitfall 5 explicitly recommends (a) — "the formatter's signature change is one line at three call sites". Option (b) (inline the recommended-detection into the `.map()` callback) would scatter the predicate across two places and make the next visual change harder. Option (a) keeps the predicate co-located with the size/download formatting.

After applying all three edits, run `bun run build` and confirm it exits 0 (TypeScript catches the rules-of-hooks violation if option (a) were botched). Then run `bun run test` and confirm full suite green.

**Manual integration check (one-time, optional but recommended):** Start `bun run dev`. Open Settings → Summary. With the Wave 1 skeleton (`models: []`), NO model shows the `· Recommended` suffix. Edit `src/data/model-benchmarks.json` and add a single model row with `name: "phi4-mini"`, `verdict: "recommended"`, and minimal quality/speed sub-objects that satisfy the validator. Vite HMR reloads; phi4-mini now shows the suffix. Revert the JSON.
  </action>
  <verify>
    <automated>cd /Users/impera/Documents/GitHub/opennotes && bun run build 2>&1 | tail -20 && bun run test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - The hard-coded `'phi4-mini'` literal is gone from the predicate: `! grep -n "'phi4-mini'" src/components/settings/SummarySection.tsx` (zero hits)
    - The hard-coded English `" · Recommended"` literal is gone: `! grep -n "' · Recommended'" src/components/settings/SummarySection.tsx` (zero hits — VALIDATION.md per-task verify map row for BENCH-05 static check)
    - The new JSON-driven predicate is present: `grep -c "BENCHMARKS.models.some" src/components/settings/SummarySection.tsx | awk '$1>=1{exit 0} {exit 1}'`
    - The module-scope validator call is present: `grep -q "const BENCHMARKS = validateBenchmarks(benchmarksRaw);" src/components/settings/SummarySection.tsx`
    - Both i18n call sites pass `t('model_recommended')`: `grep -c "formatModelLabel(model, t('model_recommended'))" src/components/settings/SummarySection.tsx | awk '$1>=2{exit 0} {exit 1}'`
    - `formatModelLabel` signature has the second parameter: `grep -q "function formatModelLabel(model: OllamaModelInfo, recommendedLabel: string): string" src/components/settings/SummarySection.tsx`
    - No `useTranslation` was added inside `formatModelLabel` (rules-of-hooks guard — check the function body via line-range grep): the only `useTranslation` call remains inside `SummarySection` at line ~45.
    - `bun run build` exits 0 (TypeScript + Vite production build clean)
    - `bun run test` exits 0 (full Vitest suite green; benchmarks.test.ts unchanged from Plan 01)
    - Settings tests (`src/contexts/OllamaSetupContext.test.tsx` and any other settings-touching tests) continue to pass.
    - Static asset is bundled by Vite: `bun run build && grep -c "phi4-mini" dist/assets/*.js | awk -F: '{s+=$NF} END{exit !(s>=0)}'` (the skeleton has `models: []` so this is 0 hits in Wave 2; the assertion is structurally that the build succeeds — the live grep-for-`phi4-mini` happens in Plan 05's verification after the live JSON is committed).
  </acceptance_criteria>
  <done>BENCH-05 closed: the badge predicate is data-driven, no hard-coded `phi4-mini` string remains in the predicate, i18n-driven label, build + tests green, no UI regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static JSON → React component module-load | `import benchmarksRaw from '../../data/model-benchmarks.json'` happens at module-eval time. Untrusted shape (a committed-but-broken JSON) crosses into the React app's startup path. |
| i18n key → user-facing string | `t('model_recommended')` resolves to English "Recommended" or Polish "Zalecany" depending on locale; both keys are present per `src/i18n/locales/{en,pl}/settings.json:147`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Tampering (continued from Plan 01) | `validateBenchmarks(benchmarksRaw)` at module scope | mitigate | Synchronous throw at module-load time IS the mitigation — fail-fast surfaces the bad JSON in `bun run build` before a release artifact is produced. Per RESEARCH §Open Question 4, this is the intentional design: blast radius is bounded to the SummarySection module, the entire app does not boot if the JSON is malformed (which catches the broken commit in CI). |
| T-20-02 | Tampering | `verdict` field with a typo (e.g., `"Recomended"`) | mitigate | The `assertVerdict` guard in Plan 01's validator throws with the path `$.models[N].verdict`. Plan 02 inherits the protection automatically — the module-scope `validateBenchmarks` call surfaces the typo at build time. |

(T-20-03 and T-20-04 do not apply to Plan 02; they live in the harness execution Plan 05.)
</threat_model>

<verification>
- `! grep -n "'phi4-mini'" src/components/settings/SummarySection.tsx` (no hits — BENCH-05 static check from VALIDATION.md)
- `grep -c "BENCHMARKS.models.some" src/components/settings/SummarySection.tsx` returns ≥ 1
- `bun run build` exits 0 (catches rules-of-hooks violation if D-30a were botched)
- `bun run test` exits 0
- Existing snapshot/integration tests that render SummarySection continue to pass.
</verification>

<success_criteria>
- BENCH-05 closed: SummarySection.tsx displays the badge from a JSON lookup, no hard-coded `phi4-mini` literal in the predicate.
- ROADMAP P20 Success Criterion 4 satisfied structurally — the predicate IS the data; Plan 05's live JSON proves the loop end-to-end.
- No UI redesign (D-32): inline `· Recommended` suffix is byte-identical to today's visual.
- i18n side-bug closed: `t('model_recommended')` replaces the hard-coded English literal.
- Build + tests green.
</success_criteria>

<output>
After completion, create `.planning/phases/20-benchmark-rerun-and-settings-recommendation-ui/20-02-SUMMARY.md` describing:
- Exact lines changed (before/after for `formatModelLabel`, the call site updates, and the new module-scope imports)
- The decision to pick D-30a option (a) over (b), with the rationale (signature change is cleaner than scattering the predicate)
- Confirmation that `bun run build` and `bun run test` are both green
- Note that the badge is invisible until Plan 05 ships live JSON — Wave 1's skeleton has `models: []` so no model matches `verdict === 'recommended'`. This is correct degraded behavior per D-33.
</output>
